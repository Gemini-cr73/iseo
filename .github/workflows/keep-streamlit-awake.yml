name: Keep ISEO Awake

on:
    schedule:
        - cron: "40 */6 * * *"
    workflow_dispatch:

jobs:
    ping:
        runs-on: ubuntu-latest
        steps:
            - name: Ping ISEO
              run: |
                  curl -L --max-time 60 https://YOUR-ISEO-URL.streamlit.app || true
